[project]
name = "agentlens-api"
version = "0.1.0"
description = "AgentLens API backend"
requires-python = ">=3.10"

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
pythonpath = ["."]
